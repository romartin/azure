---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
#
# ----------------------------------------------------------------------------
- name: Prepare random number
  ansible.builtin.set_fact:
    storage_account_name: "st{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"
    batch_account_name: "ba{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"
  run_once: true

- name: Create Storage Account
  azure_rm_storageaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ storage_account_name }}"
    location: eastus
    account_type: Standard_LRS

- name: Gather Resource Group info
  azure.azcollection.azure_rm_resourcegroup_info:
    name: "{{ resource_group }}"
  register: __rg_info

- name: Set location based on resource group
  ansible.builtin.set_fact:
    location: "{{ __rg_info.resourcegroups.0.location }}"

- name: Create User Managed Identities
  azure_rm_resource:
    resource_group: "{{ resource_group }}"
    provider: ManagedIdentity
    resource_type: userAssignedIdentities
    resource_name: "{{ item }}"
    api_version: "2023-01-31"
    body:
      location: "{{ location }}"
    state: present
  loop:
    - "ansible-test-batchaccount-identity"
    - "ansible-test-batchaccount-identity-2"

- name: Set identities base path
  ansible.builtin.set_fact:
    identity_base_path: "/subscriptions/{{ azure_subscription_id }}/resourcegroups/{{ resource_group }}/providers/Microsoft.ManagedIdentity"

- name: Set identities IDs to test. Identities ansible-test-batchaccount-identity and ansible-test-batchaccount-identity-2 have to be created previously
  ansible.builtin.set_fact:
    user_identity_1: "{{ identity_base_path }}/userAssignedIdentities/ansible-test-batchaccount-identity"
    user_identity_2: "{{ identity_base_path }}/userAssignedIdentities/ansible-test-batchaccount-identity-2"

- name: Create Batch Account
  azure_rm_batchaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
    location: eastus
    auto_storage_account:
      name: "{{ storage_account_name }}"
    pool_allocation_mode: batch_service
    tags:
      key1: value1
      key2: value2
    identity:
      type: UserAssigned
      user_assigned_identity: "{{ user_identity_1 }}"
  register: output

- name: Assert the resource was created
  ansible.builtin.assert:
    that:
      - output.changed
      - output.new.identity.type == "UserAssigned"
      - output.new.identity.user_assigned_identities | length == 1
      - output.new.identity.user_assigned_identities['{{ user_identity_1 }}'] is defined

- name: Create Batch Account -- idempotent
  azure_rm_batchaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
    location: eastus
    auto_storage_account:
      name: "{{ storage_account_name }}"
    pool_allocation_mode: batch_service
    tags:
      key1: value1
      key2: value2
    identity:
      type: UserAssigned
      user_assigned_identity: "{{ user_identity_1 }}"
  register: output

- name: Assert the resource wasn't changed
  ansible.builtin.assert:
    that:
      - not output.changed
      - output.new.identity.type == "UserAssigned"
      - output.new.identity.user_assigned_identities | length == 1
      - output.new.identity.user_assigned_identities['{{ user_identity_1 }}'] is defined

- name: Update user assigned identity
  azure_rm_batchaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
    location: eastus
    auto_storage_account:
      name: "{{ storage_account_name }}"
    pool_allocation_mode: batch_service
    tags:
      key1: value1
      key2: value2
    identity:
      type: UserAssigned
      user_assigned_identity: "{{ user_identity_2 }}"
  register: output

- name: Assert the resource was changed
  ansible.builtin.assert:
    that:
      - output.changed
      - output.new.identity.type == "UserAssigned"
      - output.new.identity.user_assigned_identities | length == 1
      - output.new.identity.user_assigned_identities['{{ user_identity_2 }}'] is defined

- name: Get Batch Account facts
  azure_rm_batchaccount_info:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
  register: output

- name: Assert the facts
  ansible.builtin.assert:
    that:
      - output.batch_account | length == 1
      - output.batch_account[0].tags | length == 2
      - output.batch_account[0].identity.type == "UserAssigned"
      - output.batch_account[0].identity.user_assigned_identities | length == 1
      - output.batch_account[0].identity.user_assigned_identities['{{ user_identity_2 }}'] is defined

- name: Update identity type to SystemAssigned
  azure_rm_batchaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
    location: eastus
    auto_storage_account:
      name: "{{ storage_account_name }}"
    pool_allocation_mode: batch_service
    tags:
      key1: value1
      key2: value2
    identity:
      type: SystemAssigned
  register: output

- name: Assert the resource was changed
  ansible.builtin.assert:
    that:
      - output.changed
      - output.new.identity.type == "SystemAssigned"
      - output.new.identity.user_assigned_identities is not defined

- name: Delete Batch Account
  azure_rm_batchaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ batch_account_name }}"
    state: absent
  register: output

- name: Assert that state has changed
  ansible.builtin.assert:
    that:
      - output.changed

- name: Clean up storage account
  azure_rm_storageaccount:
    resource_group: "{{ resource_group }}"
    name: "{{ storage_account_name }}"
    state: absent

- name: Destroy User Managed Identities
  azure_rm_resource:
    resource_group: "{{ resource_group }}"
    provider: ManagedIdentity
    resource_type: userAssignedIdentities
    resource_name: "{{ item }}"
    api_version: "2023-01-31"
    state: absent
  loop:
    - "ansible-test-batchaccount-identity"
    - "ansible-test-batchaccount-identity-2"
